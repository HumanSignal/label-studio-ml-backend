# pytest>=8 provides _pytest.scope (required by anyio's pytest plugin and current pytest-cov).
# pytest==6.x + transitive anyio -> ModuleNotFoundError: No module named '_pytest.scope'
pytest>=8.0.0,<10
pytest-cov>=4.1.0,<8