cuda-python
# Minimal extras; SAM2 and Grounding DINO install their own pins
accelerate==1.6.0
tqdm
safetensors==0.4.3
supervision>=0.18
# SAM2 dependencies (installed separately since we use --no-deps to prevent torch upgrade)
hydra-core>=1.3.2
iopath>=0.1.10
pillow>=9.4.0
# Grounding DINO runtime deps (torch/torchvision are pinned separately in Dockerfile)
transformers>=4.36,<4.42
timm>=0.9,<0.10
addict>=2.4
yapf>=0.40
numpy>=1.24
opencv-python>=4.8
pycocotools>=2.0.7
