version: "3.5"

services:
  redis:
    image: redis:alpine
    hostname: redis
    volumes:
      - "./data/redis:/data"
    expose:
      - 6379
  model_server:
    build: .
    environment:
      - MODEL_DIR=/data/models
      - RQ_QUEUE_NAME=default
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - LABEL_STUDIO_ML_BACKEND_V2=1
    ports:
      - 9090:9090
    depends_on:
      - redis
    links:
      - redis
    volumes:
      - "./logs:/tmp"