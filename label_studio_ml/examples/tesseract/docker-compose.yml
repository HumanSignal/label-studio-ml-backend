version: "3.8"

services:

  server:
    container_name: server
    build: .
    environment:
      - LABEL_STUDIO_HOST=http://host.docker.internal:8080
      - LABEL_STUDIO_ACCESS_TOKEN=967a701bb2bf7791f08c1cd357e73abd780609fd
    ports:
      - 9090:9090
    volumes:
      - "./data/server:/data"
      - "./logs:/tmp"
