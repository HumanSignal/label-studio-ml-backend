[pytest]
# The Docling smoke tests are synchronous; disable anyio auto-plugin loading so pytest 6.x
# from the repo test stack does not import anyio pytest support that requires _pytest.scope.
addopts = -p no:anyio
